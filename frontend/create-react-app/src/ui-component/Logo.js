// ==============================|| LOGO SVG ||============================== //
const Logo = () => {

  return (
    <img src={`${process.env.PUBLIC_URL}/img/logo.png`} alt="Let's learn" width="150" height = "60" />
  );
};

export default Logo;